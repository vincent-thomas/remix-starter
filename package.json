{
  "name": "root",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "nx affected -t build",
    "dev": "bun run --filter '*' dev",
    "lint": "nx affected -t lint"
  },
  "devDependencies": {
    "@nx/workspace": "^19.8.0",
    "nx": "19.8.0",
    "typescript": "^5.6.2"
  }
}
