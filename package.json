{
  "name": "root",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun@1.1.29",
  "scripts": {
    "build": "nx affected -t build",
    "dev": "bun run --filter '*' dev",
    "lint": "nx affected -t lint"
  },
  "dependencies": {
    "turbo": "^2.1.2"
  },
  "devDependencies": {
    "nx": "19.8.0"
  }
}
