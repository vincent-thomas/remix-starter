{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build"]
    },
    "lint": {
      "inputs": ["src"],
      "cache": true
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
